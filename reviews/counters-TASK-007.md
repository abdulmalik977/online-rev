# Script-owned counter history
- COUNTERS {"at": "2026-09-22T19:48:22+00:00", "action": "create", "note": "Admitted with zero counters", "counters": {"attempts": 0, "review_round": 0}}
- COUNTERS {"at": "2026-09-23T13:55:47+00:00", "action": "attempt", "note": "Session 1 of max 2: design then build portable sales page, signed provider-agnostic fake order and confirmation flow, local preview promotion, A8 fixtures 21-23 and launch checks. Use approved offer rev2 commercial-host restriction, no real payments, mail, credentials or TASK-006 retry.", "counters": {"attempts": 1, "review_round": 0}}
- COUNTERS {"at": "2026-09-23T15:02:00+00:00", "action": "attempt", "note": "Session 2 of max 2: add the owner-requested concept mailto line; complete local correction, launch, cancellation/export, refund and mail adapters, paid-to-sender bridge, customer enquiry HTTP controls and maintenance trial; verify and submit TASK-007 to review. No real sending, payments, external forms or new infrastructure tasks.", "counters": {"attempts": 2, "review_round": 0}}
- COUNTERS {"at": "2026-09-23T15:34:33+00:00", "action": "review", "note": "CHANGES (round 1), one bounded fix. Independent run at 90d2893: orders 31/31, sender 80/80; landing page built and inspected on mobile: consistent with offer rev 2, mailto fallback present. Attack harness (10 cases) against Store.accept: wrong secret, tampered payload with old signature, stale timestamp, signature over a different timestamp, oversize payload, and event-id reuse with different content are all rejected; replay of the same event is idempotent (1 order); email case/whitespace variant maps to the same customer. DEFECT: a second valid payment for the same preview with a different order_id raises an uncaught sqlite3.IntegrityError (orders.slug UNIQUE). The transaction rolls back, but the HTTP layer would return 500, the provider would retry indefinitely, and the second charge would exist nowhere in our records, so no refund would ever be triggered. Fix (<=30 lines + 1 test): detect an existing order for the slug before insert; record audit action duplicate_payment with the new order_id, write an owner queue item class refund with both order ids, do not create a second order, and return the existing order so the provider receives 200. Test: two valid events, different order_id, same slug -> one order, one queue item, no exception. Nothing else requested.", "counters": {"attempts": 2, "review_round": 1}}
- COUNTERS {"at": "2026-09-23T15:39:26+00:00", "action": "attempt", "note": "Owner explicitly authorized bounded REV-012 duplicate-payment correction after two build sessions; <=30 implementation lines and one regression test; resubmit for Claude round 2.", "counters": {"attempts": 3, "review_round": 1}}
- COUNTERS {"at": "2026-09-23T15:45:11+00:00", "action": "review", "note": "PASS (round 2). Verified at 43d2899: orders 32/32; attack harness rerun: duplicate payment for the same preview now returns the original order (provider gets 200), writes audit duplicate_payment and an owner_queue refund row; no second order; all forgery/replay/tamper cases still rejected; email normalization maps to the same customer. Landing page consistent with offer rev 2 with the mailto concept-request fallback. Software build phase complete: generator, sender, order flow, landing page, go-live gate. Real publication, checkout and sending stay behind the gate until owner inputs (SETUP-HOSTING, SETUP-EMAIL, postal address, payment provider). Follow-up for launch config, not a defect: orders.slug UNIQUE means a cancelled customer cannot re-order the same preview; acceptable for the pilot, revisit after first customer.", "counters": {"attempts": 3, "review_round": 2}}
